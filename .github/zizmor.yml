# zizmor configuration — https://docs.zizmor.sh/configuration/
rules:
  unpinned-uses:
    config:
      policies:
        # Every action must be pinned to a full commit SHA. Renovate's
        # pinGitHubActionDigests preset (renovate.json) keeps the digests fresh.
        '*': hash-pin
