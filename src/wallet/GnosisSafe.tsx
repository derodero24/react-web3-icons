import { Safe, SafeMono } from './Safe';

/** @deprecated Use `Safe` instead. */
export const GnosisSafe = Safe;

/** @deprecated Use `SafeMono` instead. */
export const GnosisSafeMono = SafeMono;

/** @deprecated Use `Safe` instead. */
export const GnosisSafe2 = Safe;

/** @deprecated Use `SafeMono` instead. */
export const GnosisSafeMono2 = SafeMono;
