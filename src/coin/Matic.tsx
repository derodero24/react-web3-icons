import { Pol, PolCircle, PolCircleMono, PolMono } from './Pol';

/** @deprecated Use `Pol` instead. */
export const Matic = Pol;

/** @deprecated Use `PolCircle` instead. */
export const MaticCircle = PolCircle;

/** @deprecated Use `PolMono` instead. */
export const MaticMono = PolMono;

/** @deprecated Use `PolCircleMono` instead. */
export const MaticCircleMono = PolCircleMono;
