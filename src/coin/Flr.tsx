import { Flare, FlareMono } from './Flare';

/** Alias for {@link Flare} — FLR is the ticker symbol for the Flare Network native token. */
export const Flr = Flare;

/** Alias for {@link FlareMono}. */
export const FlrMono = FlareMono;
