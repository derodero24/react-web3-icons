---
'react-web3-icons': minor
---

Rebuild two monochrome variants against the threshold reference (batch 4 of the mono redo): `TallyMono` (hollow outline planes → solid stacked planes with masked separation gaps, matching the black-and-white reading of the layered mark) and `LiquityMono` (floating swoosh → full-circle silhouette with the light wedge knocked out). `TruffleMono` and `DrizzleMono` were audited and confirmed already faithful (1.5% / 2.0% threshold disagreement) — no artwork change.
