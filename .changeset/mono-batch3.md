---
'react-web3-icons': minor
---

Mono redo batch 3 (owner-reviewed): `OptimismMono` now renders the OP Mainnet sun (arc + center diamond) and the **colored `Optimism` artwork is fixed** — a missing `fill-rule="evenodd"` was hiding the sun's red core; `RocketPoolMono` restored from an outline ring to the filled coin with rocket knockout; `StargateMono` rebuilt as petals plus the center star (visible in the colored mark on dark backgrounds); `DeBridge` (colored **and** mono) replaced with the official standalone logomark from debridge.com/brand — the previous artwork was the avatar tile, not the brand mark.
