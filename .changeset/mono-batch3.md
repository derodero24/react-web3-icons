---
'react-web3-icons': minor
---

Rebuild four monochrome variants to match their colored counterparts (batch 3 of the mono redo): `OptimismMono` (sun mark restored — arc and diamond knockout instead of a plain swoosh), `RocketPoolMono` (outline ring → filled coin with rocket knockout), `StargateMono` (petals-only mark, matching a straight black-and-white reading of the colored art), and `DeBridgeMono` (dark tile container restored around the fan).
